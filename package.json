{
  "name": "koco-loading-indicator-binding-handler",
  "version": "1.3.0",
  "homepage": "https://github.com/cbcrc/koco-loading-indicator-binding-handler",
  "authors": [
    "th3answer <vincent.maliko@cbc.ca>"
  ],
  "description": "A koco (frameworkjs) loading indicator binding handler",
  "main": "src/loading-indicator-binding-handler.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "keywords": [
    "koco",
    "koco-loading-indicator-binding-handler",
    "frameworkjs",
    "knockout"
  ],
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "koco": "^1.0.0"
  }
}
